%h1 Cars
%ul
	- @cars.each do |car|
		%li
			%h4= link_to car, car_path(car.id)
			= "PRIMARY" if car.primary?
			= link_to 'Make Primary', make_primary_car_path(car.id) if !car.primary?
			= link_to 'Edit', edit_car_path(car.id)
			= link_to 'Delete', car, method: :delete,
				confirm: 'Are you sure you wish to delete this car? This will also delete all records associated with this car, and cannot be undone.'
	= render 'cars/new'
