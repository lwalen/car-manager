<h1>Cars</h1>
<ul>
<% @cars.each do |car| %>
	<li>
	<h4><%= link_to car, car_path(car.id) %></h4>
		<%= link_to 'Edit', edit_car_path(car.id) %>
		<%= link_to 'Delete', car, 
			confirm: 'Are you sure you wish to delete this car? This will also delete all records associated with this car, and cannot be undone.', 
			method: :delete %>
	</li>
<% end %>
			<%= render 'cars/new' %>
</ul>
